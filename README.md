# Given a 4 digit integer, there will be exactly 1 number where when reversed, yields
# a value 4 times greater than itself

# This program finds that value
# P.S. technically, there are 2 values if you round down
